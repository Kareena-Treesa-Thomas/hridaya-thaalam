Drop your three recorded clips here with these EXACT filenames:

  relaxed.wav
  normal.wav
  elevated.wav

The website tries to play these automatically on each state change.
If a file is missing (like right now — this folder is empty), it
silently falls back to the browser's built-in text-to-speech.
No code changes needed once you add the files.
